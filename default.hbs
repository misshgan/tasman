<!DOCTYPE html>
<html lang="{{@site.locale}}">
<head>

    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="{{asset "built/index.css"}}" />

    <title>{{meta_title}}</title>

    {{ghost_head}}

</head>
<body class="{{body_class}} {{#is 'about'}}about-template{{else is 'services'}}services-template{{else is 'case-studies'}}case-studies-template{{/is}}">

{{> components/header}}

<main>

    {{{body}}}

</main>

{{> components/footer}}

<script src="{{asset "built/index.js"}}" defer></script>

{{{block 'scripts'}}}

{{ghost_foot}}

</body>
</html>
